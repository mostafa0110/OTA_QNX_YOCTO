BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "ota"
BBFILE_PATTERN_ota = "^${LAYERDIR}/"
BBFILE_PRIORITY_ota = "10"

LAYERDEPENDS_ota = "core raspberrypi"
LAYERSERIES_COMPAT_ota = "scarthgap"
